<header>
<h1 class="heading">Survey Builder</h1>
<hr>
<h4><span class="text-muted">1. Build Survey</span> | <span class="text-bold">2. Add Questions</span></h4>
<br>
</header>
<main class="new">
  <p>Next, fill in the questions.</p>
  <h4>Title:</h4>
  <p><%= @survey.title %></p>
  <h4>Description</h4>
  <p><%= @survey.description %><br>
  <hr>
  <% unless @question.number_range? %>
    <%= render partial: 'add_mcq', locals: {survey: @survey, question: @question} %>
  <% else %>
    <%= render partial: 'add_number_range', locals: {survey: @survey, question: @question} %>
  <% end %>
  <hr>
  <%= render partial: 'existing_questions', locals: { survey: @survey }%>
</main>
