<header>
<h1 class="heading">Survey Builder</h1>
<hr>
<h4><span class="text-muted">1. Build Survey</span> | <span class="text-bold">2. Add Questions</span></h4>
<br>
</header>
<main class="new">
  <p>Next, fill in the questions.</p>
  <h4>Title:</h4>
  <p><%= @survey.title %></p>
  <h4>Description</h4>
  <p><%= @survey.description %><br>
  <hr>
  <h4>Add a Question:</h4>
  <em>Choose which type of question to add.</em>
  <%= form_for [@survey, @question] do |f| %>
    <%= f.hidden_field :survey_id %>
    <%= f.select(:question_type, [['Mutiple Choice', 'Mutiple Choice'], ['Number Range', 'Number Range']], { class: "selectpicker" }) %><br>
    <%= f.submit "Add a New Question", class: "btn btn-primary" %>
  <% end %>
  <hr>

  <%= render partial: 'existing_questions', locals: { survey: @survey } %>
</main>
